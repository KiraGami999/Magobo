# Base URL of the Magobo backend (apps/web). For the iOS/Android
# simulators this can stay as localhost; for a physical device via Expo
# Go, set this to your machine's LAN IP, e.g. http://192.168.1.50:3000
EXPO_PUBLIC_API_URL=http://localhost:3000
